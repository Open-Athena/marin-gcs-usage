# CP manifest: cw-s3 → gcs, 2026-09-16 harmonization pass

Written from the cw-s3 worktree during the "factor the diff into commits" pass (specs/branch-parity-discipline.md, scramble log 2026-09-16 h11n). Nothing here was committed on gcs's checkout; the gcs session applies these with `/cp cw-s3` (adapted CPs, one commit each or folded as it sees fit), then marks the cursor.

Each item names the cw-s3 commit, the hunks to take, and the adaptation. "Inert" means the change does nothing on gcs today but removes an intended-looking delta from `diff cw-s3..gcs`.

## Queued (take as-is or with the noted adaptation)

1. **Store allow-lists are the union** — `c4aad0a`. `site/functions/_lib/index.ts`: `prefixes: ['listing/', 'snapshots/', 'cw-l2/']`; `site/functions/v1/files/[[path]].ts`: `['listing/', 'snapshots/', 'sweep/', 'cw-sweep/']`. One shared bucket (`oa-gcs-usage-dvx`), one list; the extra prefixes are inert on gcs (its D1 never points at `cw-l2/`, the scan browser just gains a readable prefix). Closes both files' last residual hunk.
2. **`/data/<store>/scans.json` filters by D1 for every store** — `9bca450`, `site/functions/data/[[path]].ts`: drop the `&& !scansM[1]` guard on the index-schema filter. On gcs the `cw/` listing is vestigial (`stores.ts` says the CoreWeave view is its own deployment, `/cw` redirects), so the only behavioral change is that a stray `/data/cw/scans.json` returns `[]` instead of unindexed ids. Keep gcs's `requireScope` line; cw's `requireViewer` is the auth-model delta.
3. **`/og` opens inside a lone bucket** — `d8bd295`, `site/src/OgPage.tsx`: the `initialPath` memo (root with one child → `[tree, tree.c[0]]`) passed to `<Treemap>`. Inert with six buckets; it's App's own lone-bucket rule applied to the og render.
4. **Login-wall copy from the store row** — `3069767`, `site/src/AuthGate.tsx`: `<h1>{DEFAULT_STORE.title}</h1>` / `<p>{DEFAULT_STORE.desc}</p>` in `LoginWall` (import `DEFAULT_STORE` from `./stores`). gcs keeps its "Access is limited…" / Google / one-time-PIN paragraphs; only the two hard-coded strings move. gcs's desc reads "Per-user storage ownership across the six marin-* GCS buckets." — the wall's current `<code>marin-*</code>` markup goes plain unless the row grows a JSX field.
5. **`warm.warm(url, headers, …)`** — cw-s3 `2dd8ddc` (`gcs-usage/src/gcs_usage/warm.py`): the request helper takes a `headers` dict instead of a bearer token, so a Cloudflare Access service-token pair (`CF-Access-Client-Id` / `-Secret`) and an agent bearer token are the same call. Adaptation on gcs: the `warm-cache` CLI verb builds `{"Authorization": f"Bearer {token}"}`; `test_warm.py` unchanged (it tests `plan`/`nearest_prior`). Closes `warm.py`'s auth hunk; the cw paths + sub-daily ids stay cw's.
6. **`/files` parquet cells** — `e38add0`, `site/src/FilesPage.tsx` (+35/−3): `renderCell` for the scan browser's parquet viewer — HR bytes for byte-ish columns, 8-char hex for id columns, floating `Tooltip` instead of native `title=`. Pure UI, no cw-specific column names; take whole.
7. **The last native `title=` tooltips → `Tooltip`** — `49dcc37`, `site/src/App.tsx` (+15/−13): three `title=` attributes in the header/scan controls become floating tips. App.tsx is the most-diverged file on the pair, so this is a hunk CP: the three sites are the units toggle, the scan picker's timestamp, and the GitHub link; gcs's App has the first two (its avatar-menu / `SiteNav` chrome owns the third).
8. **Digest plot: fit-to-data sparkline** — cw-s3 digest-plot commit on `gcs-usage/src/gcs_usage/digest_plot.py` (the OP image's y-range hugs the series instead of starting at 0; the diff treemap ellipsizes labels to cell width and moves the summary to the title row). cw's `digest_plot.py` is a fork of gcs's, so this is a hunk CP of the sparkline y-range + the treemap label rule; the quota line (`QUOTA_BYTES`) and framing-A text stay cw's.
9. **"Bucket lifecycle" fold on the home page** — the cw-s3 commit *cw-s3 FE: "Bucket lifecycle" fold* (the one that adds this entry): `site/src/lifecycle.ts` (`describeRule`, `lifecycleDiff`, `rulePrefix`; pure, vitest-covered), `site/src/LifecycleFold.tsx` (a `<details>` fold under About: the scan's `<store.base>/<scan>/lifecycle.json` — the S3 `Rules[]`, sorted by ID — as rule · prefix · action · status, diffed against the previous scan's file with `new` / `removed` / `changed` chips, the previous action on a floating `Tooltip`; "no snapshot for this scan" when the file is absent), the `details.lifecycle` scss block, and the `<LifecycleFold store asof prevScan note>` mount after About in `App.tsx`. Store-generic by construction (the path comes from `store.base`); on gcs the job would need to `pull` rules into `snapshots/<scan>/lifecycle.json` (GCS lifecycle → the same normalized shape), and the `note` prop names where gcs tracks its intended state. The `RECORDED_FROM` constant in the fold is the cw date; make it a store field if gcs starts recording on a different day.
10. **Cloud-neutral rename of the overlay package** — the cw-s3 commit *Rename `gcs-usage/` → `cloud/`, `gcs_usage` → `dt_cloud`, CLI `gcs-usage` → `dt-cloud`* (Ryan's go, 2026-09-16). Apply the identical mapping on gcs so the branches keep one layout:

    | was | now |
    |---|---|
    | dir `gcs-usage/` | `cloud/` (`git mv`) |
    | package `gcs_usage` | `dt_cloud` (`git mv cloud/src/gcs_usage cloud/src/dt_cloud`; anticipates `disk_tree.cloud` upstream) |
    | distribution `marin-gcs-usage` (`pyproject.toml` `name`, `uv.lock`) | `dt-cloud` |
    | entry point `gcs-usage = "gcs_usage.cli:main"` | `dt-cloud = "dt_cloud.cli:main"` |
    | every `gcs-usage <verb>` command string and prose mention of the CLI | `dt-cloud <verb>` |

    Touchpoints on cw-s3 (gcs's equivalents in parentheses): `Dockerfile` (`COPY cloud/…`, `pip install "./cloud[plot]"`), `.dockerignore` / `.gcloudignore` (`cloud/.venv`, `cloud/tmp`), `.github/workflows/ci.yml` (job `dt-cloud CLI tests`, `cd cloud`), `job/build.sh` + `job/cw-run.sh` (gcs: `job/run.sh`, `job/batch-submit.sh`, the digest / discord / weekly command lines), `scripts/branch-audit` `SURFACES` and `.claude/cp.yml` `surfaces` (`cloud/src`), `site/functions/api/sweep/{dispatch,purge,undo}.ts` command strings (gcs: its sweep dispatch), `site/functions/_lib/{edgeCache,index}.ts` comments, `site/migrations/0005_index_footer.sql` comment (gcs: its index migrations), `site/src/App.tsx` (the lifecycle note), `site/src/identities.gen.ts` header + `cloud/scripts/gen-identities-ts`, `site/src/AttributionRules.tsx` `YAML_URL`, `README.md` command examples, all `from gcs_usage…` imports + docstrings in `cloud/src` and `cloud/tests`. A regex pass with lookarounds did it in one go: `gcs_usage` → `dt_cloud` everywhere; `gcs-usage/` (not preceded by `-`, `/`, `#`, `:`) → `cloud/`; `./gcs-usage` → `./cloud`; `gcs-usage` followed by a space + verb / "CLI" / a backtick → `dt-cloud`.

    **Deliberately NOT renamed** (infrastructure identifiers that happen to contain the string): the data bucket `oa-gcs-usage-dvx`, the D1 `oa-gcs-usage-auth`, the Pages project `gcs-usage-icons`, the image `gcs-usage-snapshot:<tag>`, the service accounts `gcs-usage-job@` / `gcs-usage-dispatch@` / `gcs-usage-browse@`, the Slack channels `#gcs-usage*` and `gcs-usage-staging`, the Secret Manager names, the warm-cache `User-Agent`, the use-kbd `storageKey: 'gcs-usage'` and the `gcs-usage:*` localStorage keys (renaming them would reset every viewer's prefs), `site/package.json` `"name"`, and the GitHub repo `Open-Athena/marin-gcs-usage`. Also untouched: `src/disk_tree` (kept byte-identical to upstream; its three mentions are historical path prose + an internal DuckDB view name) and `specs/` history.
11. **The de novo factor's generalizations** — branch `denovo/cw-s3-gcs-2026-09-16`, tip `59433de` (ledger: "De novo factor"). `git diff gcs denovo/cw-s3-gcs-2026-09-16 -- . ':!specs' ':!pnpm-lock.yaml'` is 44 files, +534/−504, and every hunk is a generalization gcs can take as-is (the tip contains nothing cw-specific): every API gate reads the deployment's base scope (`requireViewer` = `requireScope(ctx, baseScope(env))`; `subtree`/`diff`/`path-index` drop the vestigial `cw/`-prefix scope switch); `_lib/auth.ts` grows `EDGE_TRUSTED`/`BASE_SCOPE`/`ROOT_LABEL`/`GCP_SA_KEY` in `Env`, `Identity.admin`, `isAdmin`, and the `requireViewer`/`requireAdmin` aliases; `_lib/view.ts` names the root from `ROOT_LABEL`; `data/[[path]].ts` D1-filters every store's listing; `v1/files` allow-list union; `Store` gains `marks`/`sweep`/`lifecycle`/`peer` and `SiteNav`/`Root`/`title.ts`/`About`/`UserPage`/`App` read the store (title, brand, peer link, ledger pages + nav gated on `marks`, `useDocTitle()` on home); `ChildrenTable` drops the `read`/`owner(s)` columns when no row carries them; `AuthGate`'s wall copy from the store; client `auth.ts` `VITE_AUTH_MODE`; `cli.py` loses the dead `alert` (undefined `_snapshot_dates`/`_load_meta`), gains `prepare_listing` lazy wrapper placement + `INDEX_VARIANTS` from `index_footer` + `warm-cache` dual auth and env roots; `index_footer.py` env-driven `D1_DB_ID`/`D1_DB_NAME`/`INDEX_VARIANTS` (+ the variant contract); `warm.py` `WARM_PATHS`; `weekly.scan_dates` sub-daily ids; `test_warm.py` sub-daily cases; `pyproject.toml` with the `sheets` extra beside `plot` (the tip carries gcs's extras); Dockerfile `JOB` build arg + `cloudbuild.yaml` `_JOB` + `job/build.sh` tag by job; `site/deploy`/`dev`/`cf-status` read `wrangler.toml`/`package.json`; CI on `[gcs, cw-s3]` + the `@rdub/treemap` job; `scripts/branch-audit` surfaces union. Apply as one adapted CP of the tip diff (it is small enough to read), or cherry-pick the feature commits' hunks; either way gcs's behaviour is unchanged with every default.

## Considered, not queued

- **Morning-scan day rule (`REPLY_HOUR_UTC`)** — cw-specific: gcs publishes one date-only scan a day; the rule chooses which of two sub-daily scans a day's reply reports.
- **`--redo-replies`** (post-new-then-delete-old, dry-run default) — the mechanism is general (thrds `SlackClient`), but gcs's digest replies are month threads with a different state file; port when gcs next needs a reply rewrite.
- **`sweep expire-manifest` / `require_versioning` (`-G`)** — `sweep.py` is cw's plan-first CAIOS executor (gcs has `sweep_plan`/`sweep_exec`, owner-slice). A TTL-regex expiry manifest over the layer-2 parquet would apply to GCS `tmp/ttl=` paths, but it'd land as a `sweep_plan` verb, not a hunk CP. The versioning guard is CAIOS-only (GCS has soft delete).
- **`gcs-usage lifecycle`** (`9106aaa`, the parent's) — bucket lifecycle rules as a tracked file; CAIOS-specific today.

## After these land

`site/functions/_lib/index.ts`, `v1/files/[[path]].ts`, `OgPage.tsx` and `warm.py`'s auth hunk drop out of `diff cw-s3..gcs`; `data/[[path]].ts` and `AuthGate.tsx` keep exactly one hunk each (the auth model). Re-run `scripts/branch-audit` and the factored-branch script (`wt/cw-s3/tmp/factor.py`) to confirm, then set the cw-s3 cursor on gcs.

- **`/api/series` meta.json fallback (root series)** — scans present as snapshot dirs but absent from `index_schema` get a whole-bucket point from `meta.json` (`total_bytes`/`total_objects`); sub-prefix series stay index-only. gcs: same gap for pre-2026-08-26 snapshots; port with `SNAPSHOTS_PREFIX = 'snapshots/'` (cw-s3 `functions/api/series.ts`, commit below).
