[project]
name = "marin-gcs-usage"
version = "0.1.0"
description = "Per-user attribution and reporting for Marin GCS storage"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1",
    "duckdb>=1.0",
    "fsspec>=2024.0.0",
    "gcsfs>=2024.0.0",
    "google-cloud-storage>=2.14",
    "pandas>=2.0",
    "pyarrow>=16.0",
    "pyyaml>=6.0",
    # `gcs-usage digest` drives Slack via thrds; pinned to the `py` branch (has
    # editable image blocks + per-message sender). GitHub tarball (not git+…) so
    # `pip install ./marin` works in the git-less python:slim image. Bump the
    # SHA to update.
    "thrds @ https://github.com/runsascoded/thrds/archive/200e1fd94d51832b872d30b4604e101afe429711.tar.gz",
]

[project.optional-dependencies]
wandb = ["wandb>=0.19"]
sheets = ["gspread>=6.0"]  # `sheet-push` → Google Sheets (ADC auth)

[project.scripts]
gcs-usage = "gcs_usage.cli:main"

[dependency-groups]
test = ["pytest>=8.3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
# the direct-URL `thrds` pin above
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/gcs_usage"]

[tool.pytest.ini_options]
testpaths = ["tests"]
